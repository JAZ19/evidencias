<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title><%=titulo%></title>
  </head>
  <body>
    <% for(var i = 0; i < items.length; i++) { %>
      <li><%= items[i].id %> -- <%= items[i].name %></li>
    <% }%>
    <form action="/carrito/deleteProducto" method="GET">
    	<input type="text" name="producto" required>
    	<button>Agrega producto</button>
    </form>
  </body>
</html>
